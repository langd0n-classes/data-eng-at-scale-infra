apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: kafka-${TEAM_NAME}-pdb
  namespace: ${TEAM_NAMESPACE}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      strimzi.io/name: kafka-${TEAM_NAME}-dual-role

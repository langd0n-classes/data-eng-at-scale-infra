# Multi-Tenant Kafka + NiFi Infrastructure Configuration
# Copy this to config.env and edit with your actual values

# ============================================================================
# NAMESPACE MAPPING
# ============================================================================
# ROLE                    NAMESPACE NAME              DESCRIPTION
# Shared Infrastructure   infra                       Shared Kafka, NiFi, Event Generator
# Team Namespaces         team-01, team-02, etc.      Individual team project spaces
# ============================================================================

# Infrastructure namespace - SHARED INFRASTRUCTURE
# IMPORTANT: You must create this namespace before deploying
INFRA_NAMESPACE=infra

# Team namespace prefix (teams will be named ${TEAM_NAMESPACE_PREFIX}-01, -02, etc.)
# Optional if using a single shared cluster without per-team namespaces
TEAM_NAMESPACE_PREFIX=team

# Kafka configuration
KAFKA_CLUSTER_NAME=kafka-cluster

# NiFi configuration
NIFI_CLUSTER_NAME=nifi-cluster
NIFI_IMAGE=apache/nifi:latest

# Topic prefix for multi-tenancy (used by event generator)
TOPIC_PREFIX=events

# Storage class for PVCs (adjust to your cluster's available storage classes)
# Examples: gp2, gp3, standard, hostpath
STORAGE_CLASS=standard

# Number of teams/tenants to support (optional for single-cluster deployments)
NUM_TEAMS=10

# External Kafka access (if using NodePort)
KAFKA_EXTERNAL_HOST="your-cluster-external-ip"
KAFKA_EXTERNAL_PORT="30092"

# ----------------------------------------------------------------------------
# Event Generator defaults
# ----------------------------------------------------------------------------
# Mixed event types intended for downstream splitting by event_type into
# subcategories. Configure stream types and topic naming via ConfigMap.
# For single-cluster mode, set KAFKA_BOOTSTRAP_SERVERS and optionally TOPIC.
# For multi-team mode, set TEAM_BOOTSTRAP_SERVERS.

# Example (single-cluster):
# KAFKA_BOOTSTRAP_SERVERS="kafka-cluster.infra.svc.cluster.local:9092"
# EVENT_RATE_PER_SEC="10"
# EVENT_STREAMS="symptom_report,clinic_visit,environmental_conditions"
# TOPIC="events.raw"

# Example (multi-team):
# TEAM_BOOTSTRAP_SERVERS="team01=kafka-team01.team-01.svc.cluster.local:9092,team02=kafka-team02.team-02.svc.cluster.local:9092"
# EVENT_RATE_PER_SEC="10"
# RATE_PER_TEAM="false"
# TOPIC_PREFIX="events.team"
# TOPIC_SUFFIX=".raw"
